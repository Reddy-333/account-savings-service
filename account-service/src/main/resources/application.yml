server:
  port: 8081
spring:
  application:
    name: Account Service
  datasource:
    url: jdbc:mysql://localhost:3306/project
    username: root
    password: Reddy-333@
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true